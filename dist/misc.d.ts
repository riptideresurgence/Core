declare class IDTransformClass {
    private _alphabets;
    private convert;
    toShort(inputStr: string): string;
    toNumber(inputStr: string): string;
    constructor(alphabet: string, decimals: string);
}
export { IDTransformClass };
